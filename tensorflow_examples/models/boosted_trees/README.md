# Boosted Trees - Model Migration

 ### Features
 - In the newest release of Tensorflow, `tf.gfile` was rehomed within `tf.io` as `tf.io.gfile`
 - `tf.logging` was removed in favor of the now open-source absl-py